import React, {useState, useEffect} from 'react';
import {StyleSheet, FlatList, View, Text} from 'react-native';

export default function SettingPage(props) {
  useEffect(() => {
  });

  return (
    <View>
      <Text>Welcome SettingPage</Text>
    </View>
  );
}
const styles = StyleSheet.create({
  container: {
  },
});
